spring:
  application:
    name: jc-club-auth-dev
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 117.72.73.25:8848
        prefix: ${spring.application.name}-${spring.profiles.active}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        server-addr: 117.72.73.25:8848
        enabled: true
